# Dev: omit this line — Nuxt proxies /api to the backend (no CORS, cookies on the UI port).
# Prod / direct API: set the public API origin (no trailing slash).
# NUXT_PUBLIC_API_BASE_URL=https://api.example.com
